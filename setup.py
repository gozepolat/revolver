from setuptools import find_packages
from setuptools import setup

setup(
    name='revolver',
    version='0.1.0',
    description='Evolving reusable modules',
    author='gozepolat',
    packages=find_packages(),
)